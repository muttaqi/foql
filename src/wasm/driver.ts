var props: Map<string, string> = new Map<string, string>();
var pipeline: string[][];

function getProp(key: string) {
    return props[key];
}

function getStage(i: i32, j: i32) {
    return pipeline[i][j];
}

function parse(foq: string) {

}
